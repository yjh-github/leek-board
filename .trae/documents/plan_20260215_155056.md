# 基金看板修复计划

## 发现的问题

1. **后端数据获取问题**：天天基金API可能无法正常获取数据，需要备用方案
2. **TypeORM 用法问题**：使用字符串获取 repository 不规范
3. **缺少 .env 文件**：server 目录下没有 .env 文件
4. **前端代理配置**：需要恢复代理配置以正确访问后端API
5. **定时任务可能有问题**：需要确保定时任务能正常工作

## 修复步骤

### 后端修复

1. 创建 server/.env 文件
2. 修复 fundService.ts - 添加更可靠的基金数据获取方式（备用JSON数据源）
3. 修复 index.ts - 使用正确的 TypeORM API
4. 确保能正常启动（即使数据库未配置也有友好提示）

### 前端修复

1. 恢复 vite.config.ts 中的代理配置
2. 确保所有类型导入正确
3. 添加错误处理和加载状态优化

### 额外改进

* 添加演示数据，方便测试

* 改进错误提示

## 验证

* 确认是否符合我的要求
* 能正常展示浏览

